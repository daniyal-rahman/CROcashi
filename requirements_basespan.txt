# BaseSpan System Dependencies

# Core dependencies
numpy>=1.21.0
scikit-learn>=1.0.0
PyYAML>=6.0

# Text processing and similarity
python-Levenshtein>=0.12.0  # Optional: provides faster Levenshtein distance
difflib  # Built-in Python module

# Vector search and similarity
faiss-cpu>=1.7.0  # CPU version of FAISS for vector similarity search
# faiss-gpu>=1.7.0  # GPU version (optional, for faster processing)

# Sentence transformers (for dense embeddings)
sentence-transformers>=2.2.0

# Database
SQLAlchemy>=1.4.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter

# Optional: for better performance
# torch>=1.9.0  # If using sentence-transformers with PyTorch backend
# transformers>=4.20.0  # If using custom transformer models

# Development and testing
pytest>=6.0.0
pytest-cov>=3.0.0
black>=22.0.0  # Code formatting
flake8>=4.0.0  # Linting

# Note: Some dependencies may have additional system requirements:
# - FAISS: May require specific CPU instruction sets
# - sentence-transformers: Downloads models on first use
# - python-Levenshtein: May require C compiler on some systems
